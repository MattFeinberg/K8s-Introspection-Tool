Cluster introspection tool has been deployed

If enabled at installation, the web interface is accesible at:

http://<machine-IP>:<NodePort>

The NodePort value is 30069 by default, but can be overridden at installation time
